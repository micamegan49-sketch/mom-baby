{
  "name": "tuajiw",
  "version": "1.0.0",
  "private": true,
  "description": "ตัวจิ๋ว — แอพดูแลแม่และลูกน้อย (PWA + Capacitor iOS/Android)",
  "scripts": {
    "build:web": "rm -rf www && mkdir -p www && cp -R index.html manifest.json sw.js css js icons img www/",
    "sync": "npm run build:web && npx cap sync",
    "sync:ios": "npm run build:web && npx cap sync ios",
    "sync:android": "npm run build:web && npx cap sync android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "@capacitor/local-notifications": "^8.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
